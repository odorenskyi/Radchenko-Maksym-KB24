#ifndef MODULESRACHENKO_H
#define MODULESRACHENKO_H

#include <string>
#include <sstream>

std::string s_calculation_to_string(int x, int y, int z) {
    std::ostringstream oss;
    oss << "[DEBUG] s_calculation: (" << x << " + " << y << " + " << z << ") = " << (x + y + z);
    return oss.str();
}

std::string head_task_9_1(int salary, int work_exp) {
    std::ostringstream oss;
    oss << "[DEBUG] head_task_9_1: Salary=" << salary << ", Experience=" << work_exp;
    return oss.str();
}

std::string task_9_2(int size_head) {
    std::ostringstream oss;
    oss << "[DEBUG] task_9_2: Size=" << size_head;
    return oss.str();
}

std::string task_9_3(int number) {
    std::ostringstream oss;
    oss << "[DEBUG] task_9_3: Number=" << number;
    return oss.str();
}

#endif // MODULESRACHENKO_H
